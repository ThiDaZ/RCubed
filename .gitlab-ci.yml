image: node:latest

cache:
  paths:
  - node_modules/

job:
  script:
  - apt-get update -qy
  - apt-get install -y ruby ruby-dev rubygems-integration
  - npm install
  - gem install --no-rdoc --no-ri dpl
  - dpl --provider=heroku --app=a3rdiscordbot01 --api-key=2a07dcce-8686-4b4c-983b-615028425799
# change app name and api key if necessary
